import { GeneralStack } from "./definitions";
import logos from "./logos"

const myStack : GeneralStack = {
    techs: [logos.next, logos.react, logos.express, logos.node, logos.ejs, logos.postgres],
    tools: [logos.typeScript, logos.tailwind, logos.docker, logos.prisma, logos.git],
    learning: [logos.mongodb, logos.zustand, logos.tanstack, logos.redis]
}


export default myStack