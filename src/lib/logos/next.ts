import NextLogo from "@/../public/logos/next.svg"
import { Logo } from "../definitions"

const logo : Logo = {
                title: 'NextJS',
                src: NextLogo,
                class: 'next',
            }

export default logo